import { Box, styled } from "@mui/material";

const BoxWithFlexBetween = styled(Box)({
    display:"flex" ,
    justifyContent:"space-between", 
    alignItems:"center",
})

export default BoxWithFlexBetween